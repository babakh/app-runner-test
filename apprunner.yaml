version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: "3.12"
  command: gunicorn --bind 0.0.0.0:$PORT server:app
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"