version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install --no-cache-dir -r requirements.txt
run:
  command: python3 -m gunicorn --bind 0.0.0.0:$PORT server:app
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"